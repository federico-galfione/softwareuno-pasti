import { SvgIdPathPipe } from './svg-id-path.pipe';

describe('SvgIdPathPipe', () => {
  it('create an instance', () => {
    const pipe = new SvgIdPathPipe();
    expect(pipe).toBeTruthy();
  });
});
