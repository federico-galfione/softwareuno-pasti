import { BasePageFormDirective } from './base-page-form.directive';

describe('BasePageFormDirective', () => {
  it('should create an instance', () => {
    const directive = new BasePageFormDirective();
    expect(directive).toBeTruthy();
  });
});
